<nav class="navbar" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <p class="navbar-item">
            <strong>Koti</strong><span class="mx-2">-</span>{{this.router.currentRoute.params.page}}
        </p>
        
        <div class="dropdown navbar-burger is-right {{if this.showMenu "is-active"}}" {{on "click" this.toggleMenu}}>
            <a class="dropdown-trigger">

                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>

            </a>
            <div class="dropdown-menu" id="dropdown-menu" role="menu">
                <div class="dropdown-content">
                    {{#each this.menus as |menu|}}
                    <LinkTo @route="page" @model={{menu}} class="dropdown-item {{if (eq this.router.currentRoute.params.page menu) "is-active"}}">
                        {{menu}}
                    </LinkTo>
                    {{/each}}
                </div>
            </div>
        </div>
    </div>

</nav>